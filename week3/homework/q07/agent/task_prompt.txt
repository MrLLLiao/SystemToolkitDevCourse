【智能体任务】在 q07/wordcount/wordcount.py 这个项目里加一个功能：
新增 --top N 命令行选项，统计所有输入文件里出现次数最多的前 N 个词并打印。
要求：
1. 先通读现有代码，理解它的参数结构与输出格式；
2. 保持现有 -l/-w/-c 行为不变；
3. 词切分小写化、忽略标点（建议 re.findall(r"[A-Za-z0-9]+") + Counter）；
4. 改完后用 testdata/sample.txt 实际运行验证（含 --top 与不带 --top 两种），
   并把验证输出记录下来。