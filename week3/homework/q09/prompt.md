【氛围编程提示词 · 给 AI 的完整需求】
帮我做一个"番茄钟 + 待办"小应用，满足：
1. 单文件 HTML，CSS 和 JS 全部内嵌，不要任何外部依赖（不引 CDN、不装框架）；
2. 番茄钟：默认 25 分钟倒计时，可开始/暂停/重置；可切换时长（25/45/60 分钟）；
3. 倒计时结束：页面提示 + 用 Web Audio 播放一段提示音；
4. 待办清单：可添加/勾选完成/删除，用 localStorage 持久化；
5. 界面干净现代：居中卡片、绿色主色调、大字倒计时；
6. 直接给我一份完整、可直接双击运行的 HTML 文件内容。
另外提一句：这只是个练习小工具，功能正确、能跑就行，不用追求完美工程。